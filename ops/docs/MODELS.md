# ServiceStatus

  Purpose:
    Represents the operational state of a managed service.

  Fields:
    - Service
    - State
    - Health
    - Image

Notes:
  This is a framework model, not a Docker model.
  Adapters are responsible for translating external data into this model.